MASTER PROMPT: SPASHTAI (FULL-STACK AI INSURANCE PLATFORM)

Build a premium, production-ready AI-powered insurance platform called SpashtAI.

CORE IDEA
"Spasht" = Clear
SpashtAI simplifies complex insurance policies into clear, visual, personalized insights using AI.

OBJECTIVE
Build a complete full-stack system including:
- Premium animated landing page
- Dashboard UI
- Multi-step onboarding form
- Firebase backend (Auth + Firestore + Storage)
- PDF processing
- Gemini AI integration
- Google OAuth login

FRONTEND DESIGN (React + Tailwind)
- Dark theme (deep navy/black)
- Accent: electric blue + soft purple gradients
- Glassmorphism UI
- Smooth animations
- Clean typography

LANDING PAGE STRUCTURE
1. Hero Section
Headline: Insurance. Finally Clear.
Subtext: SpashtAI uses AI to simplify your policy, reveal hidden risks, and show exactly what you pay — before it’s too late.
CTA: Get Started, See How It Works

2. Problem → Solution Section
Insurance is confusing → SpashtAI makes it clear

3. Features
- Coverage Visualizer
- Not Covered Highlighter
- Scenario Simulator

4. Demo
Example: Dengue case cost breakdown

5. Insurance Score
Display score + insights

6. Trust Section
Your data, your control

7. Final CTA
Upload Your Policy

8. Footer
SpashtAI + links

AUTHENTICATION
- Firebase Google OAuth
- Store user data in Firestore

USER PROFILE STRUCTURE
{
  userId,
  basicInfo: { age, gender, city },
  health: { diseases, smoking, drinking, exerciseLevel },
  financial: { salary, budget, riskAppetite },
  assets: { car, device },
  createdAt
}

POLICY STORAGE
{
  policyId,
  userId,
  fileUrl,
  extractedText,
  createdAt
}

AI ANALYSIS STRUCTURE
{
  analysisId,
  userId,
  policyId,
  coverage: [],
  exclusions: [],
  insights: [],
  score,
  createdAt
}

BACKEND SERVICES
- saveUserProfile(data)
- uploadPolicy(file)
- extractPdfText(fileUrl)
- analyzePolicy(userProfile, pdfText)
- saveAnalysis(result)
- getUserDashboard(userId)

GEMINI MODEL
gemini-1.5-pro

PROMPT
Analyze insurance policy and return structured JSON with:
- coverage
- exclusions
- personalized risks
- score

SECURITY
- Validate inputs
- Secure Firebase rules
- Handle errors properly

STRICT RULES
- No mock data
- Real integrations only
- Clean modular code

FINAL GOAL
A real AI-powered insurance platform that:
- Authenticates users
- Accepts PDFs
- Uses AI for analysis
- Displays clear insights
